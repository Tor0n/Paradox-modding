namespace = WrWrld_game_start
namespace = WrWrld_game_start_truce_and_wars

@create_pop_watch_fort_station = 15
country_event = { #
	id = WrWrld_game_start_truce_and_wars.701
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		set_truce = { target = Hivecult_evnt_trgt_country }
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.702
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.703
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		set_truce = { target = Hivecult_evnt_trgt_country type = war }
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.704
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		set_truce = { target = event_target:Hivecult_evnt_trgt_country type = war }
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.705
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		set_truce = { target = Hivecult_evnt_trgt_country type = none }
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.706
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		set_truce = { target = event_target:Hivecult_evnt_trgt_country type = none }
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.707
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		Hivecult_evnt_trgt_country = {
			set_truce = { target = Blood_Axes_evnt_trgt_country type = war }
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.708
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		event_target:Hivecult_evnt_trgt_country = {
			set_truce = { target = Blood_Axes_evnt_trgt_country type = war }
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.709
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		Hivecult_evnt_trgt_country = {
			set_truce = { target = event_target:Blood_Axes_evnt_trgt_country type = war }
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.710
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		event_target:Hivecult_evnt_trgt_country = {
			set_truce = { target = event_target:Blood_Axes_evnt_trgt_country type = war }
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.711
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		Hivecult_evnt_trgt_country = {
			set_truce = { target = war type = wg_WrWrld_total_pregalcom_war }
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.712
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		event_target:Hivecult_evnt_trgt_country = {
			set_truce = { target = war type = wg_WrWrld_total_pregalcom_war }
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.713
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		Hivecult_evnt_trgt_country = {
			set_truce = { target = wg_WrWrld_total_pregalcom_war }
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.714
	hide_window = yes
	is_triggered_only = yes

	#trigger = {
	#	has_country_flag = 
	#}
	immediate = {
		event_target:Hivecult_evnt_trgt_country = {
			set_truce = { target = wg_WrWrld_total_pregalcom_war }
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.800
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		every_war = {
			limit = {
				OR = {
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = attacker
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = defender
					}
				}
				#NOT = { #just in case
				#	any_defender = {
				#		is_same_value = root
				#	}
				#}
			}
			remove_war_participant = root
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.801
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		every_war = {
			limit = {
				OR = {
					has_country_flag = Imperium_gos
					has_country_flag = Blood_Axes_gos
				}
				OR = {
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = attacker
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = defender
					}
				}
				#NOT = { #just in case
				#	any_defender = {
				#		is_same_value = root
				#	}
				#}
			}
			remove_war_participant = root
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.802
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		OR = {
			has_country_flag = Imperium_gos
			has_country_flag = Blood_Axes_gos
		}
		every_war = {
			limit = {
				OR = {
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = attacker
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = defender
					}
				}
				#NOT = { #just in case
				#	any_defender = {
				#		is_same_value = root
				#	}
				#}
			}
			remove_war_participant = root
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.803
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		every_war = {
			limit = {
				OR = {
					has_country_flag = Hivecult_gos
					has_country_flag = Blood_Axes_gos
				}
				OR = {
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = attacker
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = defender
					}
				}
				#NOT = { #just in case
				#	any_defender = {
				#		is_same_value = root
				#	}
				#}
			}
			remove_war_participant = root
		}
	}
}
#Перемирие Культа Улья и Кровавых Топоров
country_event = {
	id = WrWrld_game_start_truce_and_wars.804
	hide_window = yes
	is_triggered_only = yes
    
    trigger = {
		has_country_flag = Blood_Axes_gos
	}
    
	immediate = {
        every_war = {
            limit = {
                OR ={
                    AND = {
                        any_attacker = { has_country_flag = Blood_Axes_gos }
                        any_defender = { has_country_flag = Hivecult_gos }
                    }
                    AND = {
                        any_attacker = { has_country_flag = Hivecult_gos }
                        any_defender = { has_country_flag = Blood_Axes_gos }
                    }
                }
            }
            set_truce = { type = war target = this }
            every_war_participant = { remove_war_participant = root }
        }
	}
	# immediate = {
		# OR = {
			# has_country_flag = Hivecult_gos
			# has_country_flag = Blood_Axes_gos
		# }
		# every_war = {
			# limit = {
				# OR = {
					# using_war_goal = {
						# type = wg_WrWrld_total_pregalcom_war
						# owner = attacker
					# }
					# using_war_goal = {
						# type = wg_WrWrld_total_pregalcom_war
						# owner = defender
					# }
				# }
				# NOT = { #just in case
					# any_defender = {
						# is_same_value = root
					# }
				# }
			# }
			# remove_war_participant = root
		# }
	# }
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.805
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		OR = {
			event_target:Hivecult_evnt_trgt_country
			event_target:Blood_Axes_evnt_trgt_country
		}
		every_war = {
			limit = {
				OR = {
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = attacker
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = defender
					}
				}
				#NOT = { #just in case
				#	any_defender = {
				#		is_same_value = root
				#	}
				#}
			}
			remove_war_participant = root
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.806
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		OR = {
			has_country_flag = Hivecult_gos
			has_country_flag = Blood_Axes_gos
		}
		war = {
			limit = {
				OR = {
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = attacker
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = defender
					}
				}
				#NOT = { #just in case
				#	any_defender = {
				#		is_same_value = root
				#	}
				#}
			}
			remove_war_participant = root
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.807
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		OR = {
			event_target:Hivecult_evnt_trgt_country
			event_target:Blood_Axes_evnt_trgt_country
		}
		war = {
			limit = {
				OR = {
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = attacker
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = defender
					}
				}
				#NOT = { #just in case
				#	any_defender = {
				#		is_same_value = root
				#	}
				#}
			}
			remove_war_participant = root
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.808
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		every_war_participant = { 
			limit = { 
				has_country_flag = Hivecult_gos
				has_country_flag = Blood_Axes_gos
				OR = {
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = attacker
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = defender
					}
				}
			} 
			remove_war_participant = root
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.809
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		every_war_participant = { 
			limit = { 
				OR = {
					has_country_flag = Hivecult_gos
					has_country_flag = Blood_Axes_gos
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = attacker
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = defender
					}
				}
			} 
			remove_war_participant = root
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.810
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		#remove_country_flag = declared_crisis
		#save_event_target_as = former_crisis
		random_war = {
			limit = {
				using_war_goal = {
					type = wg_WrWrld_total_pregalcom_war
					owner = event_target:Hivecult_evnt_trgt_country
				}
			}
			every_war_participant = {
				limit = {
					NOT = { is_same_value = event_target:Hivecult_evnt_trgt_country }
				}
			}
			every_war_participant = {
				prev = {
					remove_war_participant = prev
				}
			}
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.811
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		#remove_country_flag = declared_crisis
		#save_event_target_as = former_crisis
		random_war = {
			limit = {
				using_war_goal = {
					type = wg_WrWrld_total_pregalcom_war
					owner = event_target:Blood_Axes_evnt_trgt_country
				}
			}
			every_war_participant = {
				limit = {
					NOT = { is_same_value = event_target:Blood_Axes_evnt_trgt_country }
				}
			}
			every_war_participant = {
				prev = {
					remove_war_participant = prev
				}
			}
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.812
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		#remove_country_flag = declared_crisis
		#save_event_target_as = former_crisis
		random_war = {
			limit = {
				OR = {
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = event_target:Hivecult_evnt_trgt_country
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = event_target:Blood_Axes_evnt_trgt_country
					}
				}
			}
			every_war_participant = {
				limit = {
					NOT = { is_same_value = event_target:Hivecult_evnt_trgt_country }
				}
			}
			every_war_participant = {
				prev = {
					remove_war_participant = prev
				}
			}
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.813
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		every_war = {
			limit = {
				every_war_participant = {
					limit = {
						OR = {
							has_country_flag = Hivecult_gos
							has_country_flag = Blood_Axes_gos
						}
					}
				}
				OR = {
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = attacker
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = defender
					}
				}
				#NOT = { #just in case
				#	any_defender = {
				#		is_same_value = root
				#	}
				#}
			}
			remove_war_participant = root
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.814
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		every_war = {
			limit = {
				every_war_participant = {
					limit = {
						OR = {
							event_target:Hivecult_evnt_trgt_country
							event_target:Blood_Axes_evnt_trgt_country
						}
					}
				}
				OR = {
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = attacker
					}
					using_war_goal = {
						type = wg_WrWrld_total_pregalcom_war
						owner = defender
					}
				}
				#NOT = { #just in case
				#	any_defender = {
				#		is_same_value = root
				#	}
				#}
			}
			remove_war_participant = root
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.815
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		random_war = {
			limit = {
				using_war_goal = {
					type = wg_WrWrld_total_pregalcom_war
					owner = event_target:Hivecult_evnt_trgt_country
				}
			}
			every_war_participant = {
				limit = {
					NOT = { is_same_value = event_target:Hivecult_evnt_trgt_country }
				}
			}
			every_war_participant = {
				prev = {
					remove_war_participant = prev
				}
			}
		}
	}
}
country_event = { #
	id = WrWrld_game_start_truce_and_wars.816
	hide_window = yes
	is_triggered_only = yes

	#immediate = {
	#	set_truce = { target = event_target:Hivecult_evnt_trgt_country }
	#}
	immediate = {
		random_war = {
			limit = {
				using_war_goal = {
					type = wg_WrWrld_total_pregalcom_war
					owner = event_target:Blood_Axes_evnt_trgt_country
				}
			}
			every_war_participant = {
				limit = {
					NOT = { is_same_value = event_target:Blood_Axes_evnt_trgt_country }
				}
			}
			every_war_participant = {
				prev = {
					remove_war_participant = prev
				}
			}
		}
	}
}