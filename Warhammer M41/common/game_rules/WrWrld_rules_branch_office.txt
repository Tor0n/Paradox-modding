# This = country
can_build_branch_offices = {
	custom_tooltip = {
		fail_text = BRANCH_OFFICE_NOT_MEGACORP
		OR = {
			is_megacorp = yes
			has_civic = civic_galactic_sovereign_megacorp
		}
	}
}

# This = country
can_support_branch_offices = {
	custom_tooltip = {
		fail_text = BRANCH_OFFICE_NOT_REGULAR_EMPIRE
		is_gestalt = no
	}
	#WrWrld
	custom_tooltip = {
		fail_text = BRANCH_OFFICE_NOT_IMPERIUM
        if = {
            limit = { root = { is_imperium = yes } }
            owner = {
                OR = {
                    is_imperium = yes
                }
            }
        }
	}
    custom_tooltip = {
		fail_text = BRANCH_OFFICE_NOT_CHAOS
        if = {
            limit = { root = { is_chaos = yes } }
            owner = {
                OR = {
                    is_chaos = yes
                }
            }
        }
	}
}

# This = planet
# Root = country
can_build_branch_office_on_planet = {
	# custom_tooltip = {
		# fail_text = requires_recipient_not_fanatic_purifiers
		# exists = owner
		# owner = {
			# OR = {
				# is_same_species = root
				# NOR = {
					# has_civic = civic_fanatic_purifiers
					# has_valid_civic = civic_holy_ecclesiarchy
				# }
			# }
		# }
	# }
	custom_tooltip = {
		fail_text = NO_BRANCH_OFFICE_ON_SELF
		owner = {
			NOT = { is_same_value = root }
		}
	}
    #WrWrld
	custom_tooltip = {
		fail_text = BRANCH_OFFICE_NOT_IMPERIUM
        if = {
            limit = { root = { is_imperium = yes } }
            owner = {
                OR = {
                    is_imperium = yes
                }
            }
        }
	}
    custom_tooltip = {
		fail_text = BRANCH_OFFICE_NOT_CHAOS
        if = {
            limit = { root = { is_chaos = yes } }
            owner = {
                OR = {
                    is_chaos = yes
                }
            }
        }
	}
}

